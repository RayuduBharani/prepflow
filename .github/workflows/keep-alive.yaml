name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 */12 * * *' # Every 12 hours
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Health
        run: curl https://zhvmsyuxotxgeidhzpcf.supabase.co/health
